// 21-06-24

Q- Find if path exists- (lc-1971), basic bfs implementation

-- adjacency list

     unordered_map<int, vector<int>> umap;
        for(auto x: edges)
        {
            vector<int> temp = x;
            int u = temp[0];
            int v = temp[1];

            umap[u].push_back(v);
            umap[v].push_back(u); 
            // undirected graph
        }

    